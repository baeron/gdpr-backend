# Dependencies
node_modules

# Generated
/generated/prisma
dist

# Environment variables (IMPORTANT: never commit!)
.env
.env.local
.env.*.local

# Service account credentials (NEVER commit!)
gcp-service-account.json
*-service-account.json
*.pem
*.key

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs
*.log
npm-debug.log*

# Test coverage
coverage

# Load test results (keep .gitkeep)
tests/load/results/*.json

# Internal planning / deployment notes that MAY contain secrets
# (DEPLOYMENT.md snapshots production env vars incl. live Stripe keys).
# Keep these local-only. If you need to share, scrub secrets first and
# move the sanitised version into docs/.
DEPLOYMENT.md
MARKET_ANALYSIS.md
NEW_PLAN.md
PLAN.md
